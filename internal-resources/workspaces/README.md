# Workspace Placeholders

These directories are local Project Manager placeholders for fallback xmux rows.

They replace old external SSD cwd values so fallback workspace rendering and
terminal cwd validation do not depend on removable storage being mounted.

