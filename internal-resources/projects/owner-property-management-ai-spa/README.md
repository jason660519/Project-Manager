# Owner Property Management AI SPA Snapshot

This is a minimal internalized Project Manager sample resource.

The original external project directory was large and contained credentials,
local logs, dependency folders, generated output, and worktrees. Project Manager
only needs the dashboard config snapshot for bundled sample hydration, so this
directory intentionally contains only `.project-manager/config.json`.

