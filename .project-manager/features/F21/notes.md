# F21 Notes

- Keep inference deterministic and cheap; avoid model calls in fallback.
- Preserve user-authored `locatedSection` values as source of truth.
- Use path-derived hints that are stable across repos.

