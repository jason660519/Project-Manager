# F55 Notes

- User requested this feature as a PM requirements-change plan before runtime
  implementation so multiple engineers can develop from a shared source of
  truth.
- Product naming should avoid hard-coded "Development Progress" for every
  discipline. Use sheet-specific labels.
- Supabase support must include local Docker and self-hosted options, not only
  Supabase Cloud.

